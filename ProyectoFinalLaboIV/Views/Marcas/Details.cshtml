@model ProyectoFinalLaboIV.Models.Marca

@{
    ViewData["Title"] = "Detalles de la marca";
}

<h2>Detalles de la marca</h2>

<div class="table">
    <hr />
    <dl class="row">
        <dt class = "col-sm-4">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<hr />
<div>
    <a class="btn btn-info" asp-action="Edit" asp-route-id="@Model.Id">Editar</a>
    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model.Id">Borrar</a>
    <a class="btn btn-secondary" asp-action="Index">Volver a la lista</a>
</div>
